# 公考学习 Skill 需求文档

> 目标：在现有"错题管理"能力之上，扩展为**错题 + 知识点 + 方法论**三位一体的复习系统，借助艾宾浩斯遗忘曲线 + 薄弱点加权出题 + 定时推送，最大化考生的复习效果。

---

## 1. 背景与现状

### 1.1 已实现能力（SKILL.md 现有）
- ✅ **错题输入模式**：截图 + 5 标签（题型/考点/错误原因/正确答案/来源）→ 结构化入库
- ✅ **错题复习模式**：艾宾浩斯 6 阶段曲线，5 题一组，描述性回答判题
- ✅ **批量导入模式**：目录截图 → deepseek-ocr → 自动入库 + 质量报告
- ✅ **管理操作**：查询进度 / 修改 / 删除，index.md 单一数据源

### 1.2 本次完善方向
基于 `task.md` 初稿四条诉求，细化为以下六大模块。

---

## 2. 需求总览

| 编号 | 模块 | 核心价值 | 优先级 |
|------|------|----------|--------|
| R1 | 错题智能分类 | 降低手动打标签成本 | P0 |
| R2 | 知识点复习模块 | 公式/考法/总结也参与间隔复习 | P0 |
| R3 | 定时输出 | 每日复习包 + 每周报告自动推送 | P1 |
| R4 | 智能组卷出题 | 遗忘曲线 + 薄弱正确率加权 | P0 |
| R5 | 方法论文件管理 | 长文档 & 卡片两种形态，可被引用 | P1 |
| R6 | 统计与可视化 | 薄弱考点 Top N、正确率趋势 | P2 |

---

## 3. 详细需求

### R1. 错题智能分类

**痛点**：当前输入模式要求用户手动提供 5 个标签，成本高、易遗漏。

**需求**：
- 输入错题图片后，先用 `vision_analyze` / OCR 识别题干、选项、题型特征
- 自动推断：
  - **题型**：5 大模块（言语/数量/判断/资料/常识）+ 可选扩展科目（申论/公安/面试）
  - **考点**：基于预置关键词→考点映射表（可扩展，与批量导入复用）
  - **正确答案**：尝试识别红笔手写 / 题干标注；不可靠时标「待校验」
  - **错误原因 / 来源**：默认「待补充」，用户可后续批量补
- 推断结果以草稿形式展示，用户确认或一键修正后再入库

**验收**：单张截图输入，用户最少只需确认即可入库，无需手填全部字段。

---

### R2. 知识点复习模块

**需求**：管理三类知识点，独立艾宾浩斯队列，与错题并行复习。

**知识点类型**：
| 类型 | 示例 | 形态 |
|------|------|------|
| 公式卡片 | `增长率 = (现期-基期)/基期` | 正面公式 / 反面适用条件 |
| 考法/方法论 | 「逻辑填空三步骤」「图形推理口诀」 | 短卡片 |
| 知识总结 | 「常识判断-法律高频考点汇总」 | 中长文档 |

**目录结构**（与错题库平级）：
```
考公/
├── 错题库/          # 已有
├── 知识点库/
│   ├── index.md     # 知识点索引 + 艾宾浩斯状态
│   ├── 公式/
│   ├── 考法/
│   └── 总结/
└── 方法论/          # 见 R5
```

**复习机制**：
- 每张知识点卡片自带 `review_stage / next_review / review_history`
- 复习时展示卡片正面，用户回想后翻面核对，自评「记得 / 模糊 / 忘了」
  - 记得 → stage +1
  - 模糊 → stage 不变，缩短下次间隔
  - 忘了 → 重置 stage 0
- **与错题联动**：某考点知识点连续 N 次失败 → 自动把该考点未掌握错题加入下一轮复习包

**验收**：可单独说"复习知识点"，也可与错题混合出题。

---

### R3. 定时输出

**每日推送（默认每日 1 次，可配置）**：
- 汇总当天到期：错题 N 题 + 知识点 M 张
- 组成「今日复习包」一次性下发
- 复习完成后输出当日小结（通过率、新掌握数）

**每周报告（默认每周日，可配置）**：
- 本周复习题量、整体通过率趋势
- 薄弱考点 Top 5（按失败次数 / 低正确率）
- 新入库错题分布
- 下周到期预告

**触发方式**：
- 主动询问："今天有什么要复习的？" → 推送每日包
- 自动：若接入定时任务，按设定时间推送（需确认运行环境是否支持）

> ⚠️ 待确认：运行环境是否能跑后台定时任务？若不能，则退化为「用户主动唤起时聚合输出」。

---

### R4. 智能组卷出题

**出题权重公式（草案）**：
```
score(题) = 到期紧急度权重 + 薄弱考点权重 + 历史失败惩罚
```
- **到期紧急度**：Overview 越久 / stage 越低 → 权重越高（沿用现有逻辑）
- **薄弱考点权重**：该考点历史正确率越低 → 权重越高
- **历史失败惩罚**：最近一次失败的题目优先再练

**两种组卷模式**：
1. **日常复习模式**：5 题一组，遗忘曲线 + 薄弱加权混合抽题（默认）
2. **模拟考试模式**：按真实考试题型比例抽题（如行测 135 题比例缩放为 20 题迷你模考），用于周末自测

**动态调整**：每轮复习后更新各考点正确率，下轮出题权重随之变化。

**验收**：连续复习 1 周后，薄弱考点的出现频率明显高于已掌握考点。

---

