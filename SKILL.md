---
name: exam-mistake-manager
description: "考公错题自动化整理：输入模式（截图+标签→结构化错题文件）、复习模式（艾宾浩斯遗忘曲线抽题）、批量导入模式（目录截图→OCR解析→自动入库）、知识巩固模式（入题后自动从 Wiki LLM MCP 拉取相关知识点 + 历史同类错题对比）、考前速查整理（错题库薄弱点分析 + Wiki 知识检索 + 三模式：完整指南/单概念速查/题型方法论）。"
category: productivity
scripts: ./scripts/
---

# 考公错题自动化整理

## 目录结构

```
{OBSIDIAN_ROOT}/考公/错题库/         ← 由 config.py 的 MISTAKE_ROOT 控制
├── index.md                          ← 总览页（自动聚合，仅含各题型统计+链接）
├── index-判断推理.md                 ← V2 分索引（每题型独立，约 40 行，查找快 7x）
├── index-资料分析.md
├── index-数量关系.md
├── index-言语理解.md
├── index-常识判断.md
├── index-公安专业知识.md
├── index-申论.md
├── index-面试.md
├── 言语理解/
│   ├── 错题-001.md
│   └── screenshots/
│       └── 错题-001.png
├── 数量关系/
├── 判断推理/
├── 资料分析/
├── 常识判断/
├── 公安专业知识/
├── 申论/
└── 面试/
```

> 首次运行时调用 `config.ensure_dirs()` 自动建立全部目录和空 `index.md`。  
> 路径由环境变量 `EXAM_OBSIDIAN_ROOT` 覆盖，默认 `E:\obsidianNote\考公`。

---

## 支持的题型（ALL_TYPES）

```
言语理解 / 数量关系 / 判断推理 / 资料分析 / 常识判断
公安专业知识 / 申论 / 面试
```

传入不在此列表中的题型会抛出 `ValueError`。

---

## CLI 快速参考

所有 workflow 均可通过 `scripts/cli.py` 直接调用：

```bash
# 添加一道错题
python scripts/cli.py add \
  --type 判断推理 --point 加强削弱 \
  --reason 知识点盲区 --answer B \
  --source 2024国考行测 [--screenshot /path/to/img.png]

# 生成今日复习题（输出题目清单，不判题）
python scripts/cli.py review [--date YYYY-MM-DD] [--batch-size 5]

# 判题并更新状态
python scripts/cli.py judge "1.B 2.C 3.A 4.D 5.B" [--date YYYY-MM-DD]

# 批量导入截图目录
python scripts/cli.py import-batch /path/to/dir \
  [--reason 待补充] [--source-name 公安题库] [--dry-run]

# 查看统计
python scripts/cli.py stats

# 修改错题字段
python scripts/cli.py modify 错题-001 \
  --type 判断推理 \
  --field knowledge_point --value 图形推理

# 删除错题
python scripts/cli.py delete 错题-001 --type 判断推理
```

---

## 错题文件格式（MistakeCard → .md）

由 `mistake_manager._card_to_md()` 生成，格式固定：

```markdown
---
id: 错题-001
date: 2026-06-19
题型: 判断推理
考点: 加强削弱
错误原因: 知识点盲区
正确答案: B
来源: 2024国考行测
review_stage: 0
next_review: 2026-06-19
review_history: []
status: pending
---

## OCR 文本          ← 仅批量导入时存在，截取前 300 字
```
（OCR 识别内容）
```

![[screenshots/错题-001.png]]

## 标签
- **题型**：判断推理
- **考点**：加强削弱
- **错误原因**：知识点盲区
- **正确答案**：B
- **来源**：2024国考行测

## 复习记录
| 日期 | 结果 | 阶段 | 下次复习 |
|------|------|------|----------|
（暂无）
```

---

## index.md 格式（V2 — 分索引）

**V2 核心优化：每题型独立索引，查找速度提升约 7 倍。**

```python
# V2 按题型操作（推荐——只读 1/8 的数据）
data = index_manager.load_category_index("判断推理")
new_id = index_manager.next_id(qtype="判断推理")
index_manager.update_entry(id, stage, next_rev, status, qtype="判断推理")
due = index_manager.get_due_entries(today, qtype="判断推理")
```

### 分索引文件格式（index-判断推理.md）

```yaml
---
last_id: 295
total: 89
pending: 87
mastered: 2
updated: 2026-07-20
qtype: 判断推理
---

# 判断推理 · 错题索引

> 共 89 题 | ⏳待复习 87 题 | ✅已掌握 2 题 | 最近更新：2026-07-20

| ID | 考点 | 错误原因 | 答案 | 来源 | 阶段 | 下次复习 | 状态 |
|----|------|----------|------|------|------|----------|------|
| 错题-295 | 集合推理 | 近义词偷换 | A | 2020深圳市考 | 0/6 | 2026-07-20 | ⏳待复习 |
```

- 每题型一个文件，只含该题型的条目（约 40 行，而非原来的 300+ 行）
- frontmatter 含 `qtype` 字段标识题型
- 自动根据文件内条目重算 `total / pending / mastered`

### 主 index.md 总览页（自动生成）

```yaml
---
updated: 2026-07-20
---

# 错题库索引 · 总览

> 共 267 题 | ⏳待复习 259 题 | ✅已掌握 8 题 | 最近更新：2026-07-20

| 题型 | 总数 | 待复习 | 已掌握 | 分索引 |
|------|:---:|:-----:|:-----:|------|
| 判断推理 | 89 | 87 | 2 | [[index-判断推理]] |
| 资料分析 | 45 | 43 | 2 | [[index-资料分析]] |
```

- **自动生成**，每次操作分索引后自动重建
- 不要手动编辑（会被覆盖）
- 主索引只做总览和导航，实际数据在分索引中
- 状态列：`⏳待复习` / `✅已掌握`
- 阶段列：`N/6`，N 为 `review_stage`

---

## 艾宾浩斯遗忘曲线间隔表

由 `scheduler.py` 实现，间隔配置在 `config.EBBINGHAUS_INTERVALS`：

| 阶段（stage） | 状态 | 通过后间隔（天） | 到达方式 |
|:---:|------|:---:|------|
| 0 | 新题 / 答错重置 | 当天 | 新建 或 `fail_review()` |
| 1 | 第 1 次通过 | +1 | `pass_review(stage=0)` |
| 2 | 第 2 次通过 | +2 | `pass_review(stage=1)` |
| 3 | 第 3 次通过 | +4 | `pass_review(stage=2)` |
| 4 | 第 4 次通过 | +7 | `pass_review(stage=3)` |
| 5 | 第 5 次通过 | +15 | `pass_review(stage=4)` |
| 6 | ✅ 已掌握 | — | `pass_review(stage=5)` → `MASTERED_STAGE` |

**答错（`fail_review()`）**：任意阶段答错 → `stage=0`，`next_review = 明天`。  
**模糊记得（`fuzzy_review()`）**：`stage` 不变，`next_review` 取相邻两档间隔的中值（供扩展，当前 CLI 未暴露）。  
**已掌握**：`stage=6 >= MASTERED_STAGE(6)` → `status="mastered"`，退出复习队列，`next_review` 设为 `今天+3650天`（占位，不会被抽到）。

---

## Workflow 1：添加错题（输入模式）

### 触发条件
- 用户发送截图后，下一条消息包含标签信息
- 用户明确说"添加错题"/"记录错题"

### 必填字段（缺失时追问）

| 字段 | Python 参数 | 说明 |
|------|-------------|------|
| 题型 | `question_type` | 必须在 `ALL_TYPES` 内 |
| 考点 | `knowledge_point` | 具体知识点 |
| 错误原因 | `error_reason` | 知识点盲区 / 计算失误 / 审题偏差 / 方法不会 / 粗心 / 时间不够 |
| 正确答案 | `correct_answer` | A/B/C/D 或 √/× |
| 来源 | `source` | 试卷名称 |

### 执行步骤

```
1. 调用 config.ensure_dirs() 确保目录存在
2. 调用 index_manager.next_id(qtype=question_type) 生成新 ID
   → 读取对应题型分索引 frontmatter 的 last_id（仅 5 行，极快）
3. 调用 mistake_manager.create_mistake(...)
   → 复制截图到 {题型}/screenshots/{新ID}.png（若提供 screenshot_src）
   → 写入 {题型}/{新ID}.md
   → 调用 index_manager.add_entry(card)
      · 追加到对应题型的分索引文件（如 index-判断推理.md）
      · 自动重建主 index.md 总览页
      · last_id += 1
      · 追加一行到对应题型表格
      · 重写 frontmatter（total/pending/mastered/updated）
```

### 回复用户

```
✅ 已保存 错题-XXX
📂 {题型}/{考点}
📅 已加入复习队列，下次复习 {next_review}
```

---

## Workflow 2：复习错题（复习模式）

### 触发条件
- 用户说"复习"/"复习错题"/"来几题"/"抽题"/"继续复习"

### Step 1：扫描到期题目

```python
# review_engine.select_due_items(today)
# → index_manager.get_due_entries(today)
筛选：status != "mastered" AND next_review <= 今天
         AND knowledge_point != "待补充"   # ← 跳过空记录
         AND error_reason != "待补充"       # ← 跳过空记录
排序：(next_review ASC, review_stage ASC, random)
```

**⚠️ 过滤"待补充"条目**：早期入库的很多错题的 `knowledge_point` 和 `error_reason` 为"待补充"，没有考点和错因，复习价值极低。到期扫描时必须过滤这两项。

**当到期不足时的补位策略**：如果过滤后有内容可用的到期题目 < batch_size：
1. 从同题型选取最近的（入库日期 DESC）且 `knowledge_point != "待补充"` 的条目补位
2. 它们在艾宾浩斯计划上尚未到期但内容完整，补位复习优于无内容复习
3. 给用户说明：`（N 题到期，补 M 题近期入库题凑够一批）`

若无到期题目且无可补位：输出"今天没有待复习的错题，下一次复习在 {最近 next_review}"。

### Step 2：取一批（默认 5 题）

```python
# review_engine.take_batch(due, batch_size=config.REVIEW_BATCH_SIZE)
# 默认 REVIEW_BATCH_SIZE = 5
selected = due[:batch_size]
```

### Step 3：展示题目

**V2截图获取方法（关键！用户要求直接用原图，不经过浏览器渲染）：**

```python
# vault 截图存储格式：
#   {题型}/screenshots/错题-{ID}.png    ← 优先
#   {题型}/screenshots/错题-{ID}.jpg    ← 兼容（大量早期截图是 .jpg）
#
# V2 直接展示（用户明确要求不经过浏览器渲染）：
# 1. 复制截图到 cache 目录
#    import shutil; shutil.copy2(path, "/home/administrator/.hermes/cache/images/")
# 2. 在回复中用 MEDIA: 直接展示
#    MEDIA:/home/administrator/.hermes/cache/images/错题-{ID}.{ext}
# 3. 降级方案（若直接 MEDIA 不渲染）：
#    browser_navigate → browser_vision → MEDIA:
#
# 查找命令（同时匹配两种后缀）：
#   find "{题型}/screenshots" -name "错题-{ID}.*" 2>/dev/null
#
# 找到后直接用 MEDIA: 路径展示
```

**⚠️ 注意**：通过会话直接入库的错题（如错题-270~277、错题-282~283）可能没有 vault 截图——它们来自模考 APP 截图，未复制到 vault。这类题走文字重建（从 index 条目的考点+错因重建考点识别题）。

```
📝 判断推理复习 · 第 N 组（混合题型）

MEDIA:{截图绝对路径}    ← 有截图时直接展示

【题1】{题型子类} · {考点} · {来源}
{文字说明或直接原题}
你的答案：

...（共 N 题）

请用格式回复：1.B 2.C 3.A ...
```

### Step 4：解析答案并判题

答案解析由 `review_engine._parse_answers()` 处理，支持分隔符：`. ． ， , 、`

**判题规则（`_is_forget(answer)`）：**
- 为空 → ❌ 失败
- 包含关键词「不记得 / 忘了 / 不会 / 跳过 / 不知道 / 没印象」→ ❌ 失败
- 字符长度 ≤ 1（含单个字母）→ ❌ 失败（单字母视为没有实质内容）
- 其他任何描述性回答 → ✅ 通过

> ⚠️ **注意**：当前判题不核对正确答案字母，能描述思路即为通过。

### Step 5：更新状态

```python
# ⚠️ 注意返回值数量不同：
# pass_review(old_stage, today) → (new_stage: int, next_review: str, is_mastered: bool)  # 3值！
# fail_review(today)           → (stage: int, next_review: str)                         # 2值！

# 正确写法：
if passed:
    new_stage, next_review, is_mastered = scheduler.pass_review(old_stage, today)
else:
    new_stage, next_review = scheduler.fail_review(today)  # 不要试图解包3个值

# 每题调用：
mistake_manager.update_review_state(
    mistake_id=eid, question_type=qt, passed=passed,
    review_stage=new_stage, next_review=next_review,
    status="learning", review_date=today.isoformat(),
    old_stage=old_stage,    # ← 必填，不可省略
)
  → 重写 {题型}/{ID}.md（frontmatter + 复习记录表）
  → index_manager.update_entry(...)（更新 index.md 对应行）
```

> 🐛 **常见坑**：`pass_review` 返回 3 个值而 `fail_review` 只返回 2 个。如果用相同变量数解包会触发 `ValueError: too many values to unpack`。

### 复习模式v2：错题库主动抽题混合复习

**触发条件**：用户说「从错题库出题」「抽题复习」「出N题练练」「复习XX题型」

**与v1的核心区别**：

| 维度 | v1（到期筛选） | v2（主动抽题） |
|------|--------------|--------------|
| 选题 | 到期时间排序 | 手动选考点/题型混合 |
| 题目展示 | 原题截图+回忆 | 从index记录重建考点识别题/力度判断题 |
| 判题 | 二值（记得/不记得） | 逐题判分+讲评+关联历史错题 |
| 混合 | 仅到期，分散 | 可控混合（默认图形推理:逻辑判断=7:3） |

#### Step 1：筛选可用条目

从 index.md 读取目标题型的待复习条目，过滤：
- status != "mastered"
- knowledge_point != "待补充"
- error_reason != "待补充"

#### Step 2：从index记录重建题目

**图形推理 → 考点识别题**（考你能不能识别该用什么规律，而非计算）：
```
模板：「题干N个图形，特征为{从考点和错因提取}」
问：这道题应优先用什么规律？

选项模式：
A. 属性规律（对称/曲直/开闭）
B. 数量规律（点/线/角/面/素/复合）
C. 位置规律（平移/旋转/翻转）
D. 样式规律（加减同异/遍历）
```

**逻辑判断 → 力度判断题**（考能不能区分搭桥vs排除他因vs他因削弱）：
```
重建模板：「{从index提取论证逻辑}」
问：最能加强/削弱的是？

选项模式：
A. 搭桥/断桥（概念跳跃时优先）
B. 排除他因/他因削弱（有额外变量差异时）
C. 举例/类比（力度较弱）
D. 无关项/重复论点
```

有独立 .md 文件+截图缓存的 → 优先用原题截图出题。

#### Step 3：出题+接收答案

```
【题1/10】{考点}
{题目文字}
A. ...
B. ...
C. ...
D. ...

用户回复答案序列：如 "AACBD DCABA"
```

#### Step 4：逐题讲评+汇总

```
**题{N}** ✅|❌ | 考点：{考点}
正解：{正确选项} | 你选：{用户选项}
{错因一句话 + 对应口诀}
关联：与错题-XXX相同考点

---

## 📊 N题结果

✅ 正确：N题 | ❌ 错误：M题 | 正确率：N/10 (X%)

### 🔍 薄弱点
- {考点}：错X题，建议查阅{对应参考文件}
- {考点}：与错题-XXX同考点，集中攻克

### 📌 本轮口诀
{根据错误最多的考点给一句口诀}
```

---
### Step 6：输出结果

```
## 📊 本次复习结果

✅ 题1 错题-001 → 阶段 1→2，下次复习 2026-06-21
❌ 题2 错题-002 → 错误，重置，明天再复习
✅ 题3 错题-003 → 🎉已掌握

**通过率**：2/3 (67%)
```

---

## Workflow 3：批量导入（import-batch 模式）

### 触发条件
- 用户指定含错题截图的目录路径，要求批量入库
- 说"整理 XX 目录下的错题"/"批量导入错题"

### 执行流程

```
batch_importer.import_directory(source_dir, ...)
  ↓
1. _collect_images(source_dir)
   → 扫描 {.png .jpg .jpeg .webp .bmp}（大小写均支持）
   → 去重并稳定排序

2. 对每张图片调用 _ocr_single_image(image_path)
   → _call_ocr_api()：POST 到 config.OCR_API_URL
      · 模型：config.OCR_MODEL（deepseek-ai/DeepSeek-OCR）
      · API Key：环境变量 EXAM_OCR_API_KEY 或 config 中默认值
      · 超时：120s
   → _parse_ocr_text(raw_text, folder_name)：
      · _infer_question_type(folder_name)：目录名关键词映射题型
      · _extract_knowledge_point(text)：config.KEYWORD_MAP 关键词匹配
      · _extract_options(text)：正则提取 A/B/C/D 选项
      · _extract_answer(text)：正则提取答案标记
      · quality="ok" if has_chinese AND len(text)>20 else "poor"

3. （非 dry_run）逐题调用 mistake_manager.create_mistake()
   → OCR 文本摘要（前 300 字）写入 .md 的 OCR 文本节
   → 截图复制到对应题型 screenshots/ 目录
   → 更新 index.md

4. 返回统计字典 {total, success, poor, items}
```

### 目录名 → 题型映射（`_infer_question_type`）

| 目录名含 | 映射题型 |
|----------|----------|
| 言语 | 言语理解 |
| 数量 | 数量关系 |
| 判断 | 判断推理 |
| 资料 | 资料分析 |
| 常识 | 常识判断 |
| 公安 | 公安专业知识 |
| 申论 | 申论 |
| 面试 | 面试 |
| （未匹配） | 常识判断（默认兜底） |

### 考点关键词映射（`config.KEYWORD_MAP`，按顺序匹配首个命中）

<details>
<summary>展开完整映射表</summary>

```python
# 公安 / 行政法类
("回避", "回避制度"), ("鉴定", "鉴定程序"), ("传唤", "传唤程序"),
("证据", "证据规则"), ("处罚", "处罚程序"), ("强制", "行政强制"),
("调解", "治安调解"), ("听证", "听证程序"), ("扣押", "扣押程序"),
("行政复议", "行政复议"), ("行政诉讼", "行政诉讼"),
("治安管理", "治安管理处罚法"), ("程序规定", "办理行政案件程序"),
# 言语理解
("逻辑填空", "逻辑填空-语境分析"), ("片段阅读", "片段阅读-主旨概括"),
("语句排序", "语句排序"), ("语句填空", "语句填空"),
# 数量关系
("行程问题", "行程问题-相遇追及"), ("工程问题", "工程问题"),
("排列组合", "排列组合"), ("概率", "概率问题"), ("利润", "利润问题"),
# 判断推理
("图形推理", "图形推理"), ("类比推理", "类比推理"),
("定义判断", "定义判断"), ("逻辑判断", "逻辑判断-加强削弱"),
("加强", "加强削弱"), ("削弱", "加强削弱"),
# 资料分析
("增长率", "增长率计算"), ("增长量", "增长量计算"),
("比重", "比重计算"), ("平均数", "平均数计算"),
# 常识判断
("宪法", "宪法"), ("刑法", "刑法"), ("民法", "民法"), ("行政法", "行政法"),
```

</details>

### 质量报告格式

```
## ✅ 批量导入完成

### 📊 识别质量
| 指标 | 数值 |
|------|------|
| 总数 | N |
| 入库成功 | N |
| OCR 质量差 | M |

### ⚠️ 需要手动补充
- 错误原因：全部默认「待补充」
- 红笔手写答案：OCR 识别率有限，请重点校验
- 未匹配到关键词的考点：已标记为「待补充」

### 📋 入库明细
| 文件名 | 题型 | 考点 | 答案 | 质量 |
|--------|------|------|------|------|
| img001.png | 公安专业知识 | 回避制度 | B | ✅ |
| img002.png | 公安专业知识 | 待补充 | 待校验 | ⚠️ |
```

---

## Workflow 4：知识巩固（入题后自动触发）

### 触发条件

**每次 Workflow 1（添加错题）完成后自动执行**，无需用户额外指令。  
用户也可事后单独触发：说"巩固 错题-XXX"/"给我看看 错题-XXX 的相关知识"。

### 概述

入库完成后，通过 **Wiki LLM MCP** 完成两件事：
1. 从公考知识库中检索与该题考点相关的知识内容，辅助深度理解
2. 从 index.md 中找出相同错误原因的历史错题，暴露系统性薄弱点

两项内容拼合后一次性输出，不需要用户二次确认。

---

### MCP 基本信息

**Base URL**：由用户环境决定（Wiki LLM MCP 服务地址）  
**鉴权**：由 MCP 连接层处理，调用时无需手动附加 token  
**用到的端点**：

| 端点 | 用途 |
|------|------|
| `GET /api/v1/projects` | 列出所有 project，找到公考知识库的 `id` |
| `POST /api/v1/projects/{id}/search` | 语义 + 关键词检索相关知识片段 |
| `GET /api/v1/projects/{id}/files/content` | 按路径读取单个 wiki 文件全文（可选，用于补充完整内容） |

---

### Step 1：定位公考知识库 Project

```
GET /api/v1/projects
```

返回 project 列表，从中找到名称 / 路径包含「公考」「考公」「行测」「申论」等关键词的项目，取其 `id`。

**缓存策略**：project id 在同一会话中保持不变，第一次查到后直接复用，不重复请求。

若列表中找不到公考相关 project：跳过知识巩固步骤，在回复末尾提示：
```
⚠️ 未找到公考知识库 Project，知识巩固跳过。请在 Wiki LLM 中确认项目名称。
```

---

### Step 2：检索相关知识点

构造两次搜索，覆盖「考点维度」和「题型维度」，合并去重结果：

**搜索 A：考点精准检索**
```json
POST /api/v1/projects/{id}/search
{
  "query": "{knowledge_point}",
  "topK": 3,
  "includeContent": true
}
```

**搜索 B：题型+错误原因宽泛检索**（补充方法论层面的知识）
```json
POST /api/v1/projects/{id}/search
{
  "query": "{question_type} {error_reason}",
  "topK": 3,
  "includeContent": true
}
```

**结果处理：**
- 合并两次结果，按相关性分数去重（相同文件路径只保留分数更高的一条）
- 最多保留 **4 条**，避免信息过载
- 若 `includeContent=true` 已返回片段内容，直接使用；若返回片段过短（< 100 字），对该条目追加调用：
  ```
  GET /api/v1/projects/{id}/files/content?path={result.path}
  ```
  取全文前 800 字作为补充

**无结果时**：该部分输出"暂无匹配的知识库内容"，继续执行 Step 3。

---

### Step 3：查找相同错误原因的历史错题

直接读取已在内存中（或刚写入）的 index.md 数据，**不再发起 MCP 请求**：

```python
# 伪代码
data = index_manager.load_index()
same_reason = [
    e for e in data.entries
    if e.error_reason == card.error_reason      # 错误原因完全匹配
    and e.id != card.id                          # 排除刚入库的这道题本身
]
# 按 next_review ASC 排序（越快到期越靠前，提醒关注）
same_reason.sort(key=lambda e: e.next_review)
# 最多展示 5 条
same_reason = same_reason[:5]
```

**匹配规则**：`error_reason` 字符串完全匹配（与 index.md 存储值一致）。  
若无历史同类错题（刚开始建库时），该部分输出"暂无相同错误原因的历史错题"。

---

### Step 4：组合输出

Workflow 1 的确认消息之后，紧接着输出以下格式：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 知识巩固 · 错题-XXX（{考点}）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🔖 相关知识点（来自公考知识库）

### 1. {wiki 文件标题或路径}
{知识片段内容，≤ 300 字，超出截断并注明「…（查看完整内容：{path}）」}

### 2. {wiki 文件标题或路径}
{知识片段内容}

（共找到 N 条相关内容）

---

## 🔁 相同错误原因的历史错题（{error_reason}）

共 M 题曾因「{error_reason}」出错：

| ID | 题型 | 考点 | 来源 | 阶段 | 下次复习 |
|----|------|------|------|------|----------|
| 错题-005 | 判断推理 | 削弱论证 | 2023省考 | 2/6 | 2026-06-21 |
| 错题-012 | 判断推理 | 加强论证 | 2024国考 | 0/6 | 2026-06-19 |

💡 **建议**：这 M 道题都是「{error_reason}」，建议集中攻克 {考点} 相关方法论。
```

**若两部分均无内容**（知识库空 + 无历史错题），整个知识巩固块静默跳过，不输出分割线。

---

### 实现注意事项

- **调用顺序**：Workflow 1 的文件写入和 index.md 更新必须全部完成后，再发起 MCP 请求——避免网络超时影响入库
- **MCP 失败处理**：任何 MCP 请求失败（网络错误、500 等）均 catch 后静默跳过该部分，在输出末尾加一行 `⚠️ 知识库检索暂时不可用，可稍后手动触发巩固`，不影响错题入库本身
- **project id 作用域**：在一次对话 session 内缓存，跨 session 需重新查询
- **search 结果的 path 字段**：用于 `GET /files/content?path=` 时需 URL encode，中文路径尤其注意
- **历史错题查询完全本地**：不走 MCP，直接读 index.md，零网络延迟

---

## Workflow 5：考前速查整理（Pre-exam Quick Review）

### 触发条件

**完整指南模式**（系统性整理，一次输出全科速查表）：
- 用户说"帮我整理考前速查"/"出一份考前速查表"/"考试要到了，帮我梳理高频考点"
- 用户说"整理我错过的 XX 题型的考点"/"从我的错题库提炼薄弱点"

**速查模式**（单概念问答，临考冲刺）：
- 用户发单个概念名称，如"行政强制的种类"/"削弱题的做法"/"资料分析增长率公式"
- 用户连续追问不同概念，每条消息都是一个独立的知识点

**题型方法论模式**（单一题型系统性打法）：
- 用户说"削弱加强题怎么做"/"定义判断题的解题框架"/"资料分析题型的做法"

---

### 数据来源优先级

本 Workflow 同时使用**三个数据源**，按优先级依次读取：

| 优先级 | 数据源 | 读取方式 | 适用场景 |
|:---:|------|------|------|
| 1 | **错题库 index.md**（本地） | `index_manager.load_index()` | 用户的真实薄弱点，最优先 |
| 2 | **Wiki LLM MCP**（知识库检索） | `POST /search` + `GET /files/content` | 结构化知识内容、方法论 |
| 3 | **模型内置知识** | 直接生成 | Wiki 无覆盖时兜底，需标注来源 |

---

### 完整指南模式

#### Step 1：分析错题库薄弱点

读取 index.md，统计各维度分布，找出真实薄弱点：

```python
data = index_manager.load_index()

# 按题型统计待复习题数
type_stats = {}
for e in data.entries:
    if e.status != "mastered":
        type_stats.setdefault(e.question_type, {"pending": 0, "points": []})
        type_stats[e.question_type]["pending"] += 1
        type_stats[e.question_type]["points"].append(e.knowledge_point)

# 按考点统计频次（同一考点出现越多次越薄弱）
point_freq = {}
for e in data.entries:
    if e.status != "mastered":
        point_freq[e.knowledge_point] = point_freq.get(e.knowledge_point, 0) + 1

# 按错误原因统计（暴露系统性问题）
reason_freq = {}
for e in data.entries:
    if e.status != "mastered":
        reason_freq[e.error_reason] = reason_freq.get(e.error_reason, 0) + 1

# 排序：出现频次 DESC
top_weak_points = sorted(point_freq.items(), key=lambda x: -x[1])[:10]
top_weak_types = sorted(type_stats.items(), key=lambda x: -x[1]["pending"])
```

薄弱点定义：
- **高频错误考点**：同一 `knowledge_point` 出现 ≥ 2 次
- **系统性错误原因**：同一 `error_reason` 出现 ≥ 3 次（说明是方法论问题，不是知识点问题）
- **低阶滞留**：`review_stage <= 1` 且多次入库的考点（反复在初级阶段失败）

#### Step 2：Wiki MCP 检索各薄弱考点

对 Step 1 识别的 Top 考点（最多 8 个），逐一向 Wiki LLM MCP 发起检索：

**定位 Project**（与 Workflow 4 共用缓存的 project id）：
```
GET /api/v1/projects
→ 找到公考/行测/申论相关 project，取 id
```

**对每个薄弱考点搜索**：
```json
POST /api/v1/projects/{id}/search
{
  "query": "{knowledge_point} {question_type}",
  "topK": 3,
  "includeContent": true
}
```

若返回内容片段 < 150 字，追加读取全文（前 1000 字）：
```
GET /api/v1/projects/{id}/files/content?path={result.path}
```

**题型方法论额外搜索**：对每个题型，再追加一次方法论维度的搜索：
```json
POST /api/v1/projects/{id}/search
{
  "query": "{question_type} 解题方法 做题技巧",
  "topK": 2,
  "includeContent": true
}
```

**搜索总量控制**：所有考点搜索合计不超过 **20 次** MCP 请求，超出部分合并到相近考点的结果中或标注"参见同题型内容"。

#### Step 3：组合输出完整速查指南

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 考前速查整理 · {生成日期} · 距考试 N 天
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 📊 你的薄弱点分析

| 题型 | 待掌握题数 | 高频错考点 | 主要错误原因 |
|------|:---:|------|------|
| 判断推理 | 8 | 加强削弱(×3)、定义判断(×2) | 知识点盲区(×5) |
| 资料分析 | 5 | 增长率计算(×3) | 计算失误(×4) |
| ... | | | |

> 💡 「{top error_reason}」出现 N 次——这是方法论问题，不只是知识点问题，见下方方法论章节。

---

## 一、{题型} ⭐⭐⭐⭐⭐

> 你有 N 道该题型错题未掌握，以下是重点考点

### 🔑 核心考点速查

| 考点 | 你的错题数 | 核心要点（来自知识库） | 考场速判 |
|------|:---:|------|------|
| 加强削弱 | 3 | 因果链完整性；他因排除 | 看"论据→结论"间的跳跃 |
| 定义判断 | 2 | 抓定义核心属性；逐项比对 | 划出定义的"充要条件" |

### 🛠 解题方法论

（来自知识库：[[{wiki_path}]]）

{wiki 检索到的方法论内容，≤ 400 字，超出截断}

**速记口诀**：{一行记忆口诀，若知识库有则引用，否则自行生成}

### 📌 你的典型错题回顾

（来自错题库，stage ≤ 1 的高优先题）

| ID | 考点 | 错误原因 | 下次复习 |
|----|------|------|------|
| 错题-012 | 加强削弱 | 知识点盲区 | 2026-06-19 ⚠️今日 |
| 错题-008 | 定义判断 | 审题偏差 | 2026-06-21 |

---

## 二、{题型} ⭐⭐⭐⭐

...（同上结构）

---

## 📚 知识覆盖情况

| 薄弱考点 | 错题数 | Wiki 覆盖 | 建议 |
|------|:---:|:---:|------|
| 加强削弱 | 3 | ✅ 有详细内容 | 重点看方法论 |
| 增长率计算 | 3 | ✅ 有公式汇总 | 重做计算类错题 |
| 回避制度 | 2 | ⚠️ 仅有简述 | 补充原文条文 |
| 类比推理 | 1 | ❌ 未收录 | 建议临时查阅教材 |

---

## 🎯 考场策略建议

- **今日必做**：{next_review = 今天 的错题列表，直接给 ID}
- **薄弱题型攻克顺序**：{按 pending 数量 DESC 排列题型}
- **方法论问题**：「{top error_reason}」出现最多，{针对性建议}
- **放弃策略**：❌ Wiki 无覆盖且错题数 ≤ 1 的考点，考前不建议新学
```

---

### 速查模式（单概念冲刺问答）

**判断标准**：用户发送的消息是一个孤立的概念词或短句，且上下文显示是在快速过知识点（不是在系统整理）。

#### 每条消息的处理流程

```
1. 在 index.md 中搜索该考点是否有错题记录
   → 有：在回复末尾标注 "📌 你曾在此考点出错 N 次（最近一次：{date}）"

2. 向 Wiki MCP 发起单次检索（topK=3）
   POST /api/v1/projects/{id}/search
   { "query": "{用户发送的概念}", "topK": 3, "includeContent": true }

3. 用检索内容 + 模型知识组合回复
   → 若 Wiki 有内容：以 Wiki 为主，末尾注明 "来源：[[{path}]]"
   → 若 Wiki 无内容：用模型知识回复，末尾注明 "（此考点在你的知识库中未收录）"

4. 格式规范（移动端友好，3列以内，6行以内）
```

#### 速查回复格式

```
**{考点名}**

{核心要点，3-5 条，每条 ≤ 20 字}

或（对比类概念用表格）：

| 对比项 | A | B |
|------|------|------|
| ... | ... | ... |

**速记**：{一行口诀}

来源：[[{wiki_path}]] ｜ 📌 你曾出错 N 次
```

**速查模式禁止事项**：
- ❌ 不输出完整指南结构（无大标题、无策略章节）
- ❌ 不主动追问"需要我展开吗"——用户在冲刺，每问必答，最多在回复末尾加一行可选提示
- ❌ 表格不超过 3 列，避免移动端渲染错乱
- ❌ 不读取所有 wiki 页面，只发一次 MCP 搜索，取最佳匹配

---

### 题型方法论模式（单题型系统打法）

**判断标准**：用户要求某个题型的**完整解题框架**，而不只是一个概念解释。

#### 处理流程

```
1. 从 index.md 统计该题型的历史错题
   → 按 knowledge_point 分组，找出高频错点

2. Wiki MCP 发起两次搜索
   搜索 A："{题型} 解题方法 技巧"（topK=4）
   搜索 B："{题型} 常见考法 陷阱"（topK=3）
   → 合并结果，对最相关的 1-2 个文件追加读全文

3. 组合输出方法论框架
```

#### 方法论输出格式

```
## {题型} · 系统打法

### 🗂 题型分类
（子题型 / 考法变体，用 Wiki 内容填充）

### 🛠 通用解题框架
{步骤化流程，3-5步}

### ⚡ 考场速判技巧
{2-4条，每条一句话}

### ❗ 你的高频错点
（来自你的错题库）
| 错误考点 | 出错次数 | 核心原因 |
|------|:---:|------|
| 加强削弱 | 3 | 知识点盲区 |

### 📌 速记口诀
{一行或两行口诀，Wiki 有则引用，否则自行生成}

来源：[[{wiki_path_1}]]、[[{wiki_path_2}]]
```

---

### MCP 调用总量控制（Workflow 5 全局）

| 模式 | 最大 MCP 请求次数 | 说明 |
|------|:---:|------|
| 完整指南模式 | 20 次 | 每考点 2-3 次，最多 8 个考点 |

### 核心流程

对每张截图执行以下 5 步：

```
1. 截取关键信息
   - 图表标题（是"累计"还是"当月"？）
   - 图表类型（折线/柱状/表格？数值轴和增速轴分别是什么？）
   - 题干核心问法（同比/环比？增速/增量/比重/贡献率？）
   - 选项与用户答案（用户选了什么→错因方向）
   - 正确答案及其解析思路

2. 定位考点 + 识别错因
   考点映射（高频考点速查）：
   - "环比增速高于上年同期" → 十二、由同比判环比比大小
   - "比重上升/下降判断" → 四、比重趋势（比重上升=部分增速>整体增速）
   - "利润率变化反推利润增速" / "两期比重差逆用" → 四、两期比重逆用
   - "增长贡献率" → 五、增长贡献率≠现期占比
   - "累计数据+环比" → 十四、读图第一眼（累计→先做差）
   - "增速比较" → 看比重折线趋势，不计算
   - "乘积增长率"（总=A×B）→ 二、增长率-乘积增长率≈a+b
   - "增长量比较（各年同比 vs 年均）" → 三、增长量-各年同比＞年均？挨个算，接近≠超过
   - "平均数增长率 vs 两期比重差" → 六、平均数 vs 四、比重（对比表：结果带个百分点=比重差，带%=平均数增长率）
   - "多几倍 vs 是几倍" → 七、倍数与翻番（看到"比"字+"倍"字→条件反射-1）
   - "实验类削弱/加强" → 参考 逻辑判断-加强削弱与实验类框架.md

   错因归类（预置词库）：
   - 概念混淆（贡献率≠占比、同比≠环比、累计≠当月、总量占比≠增量贡献率）
   - 公式不熟（两期比重逆用/乘积增长率/增长贡献率公式盲区）
   - 计算失误（截位/尾数/估算放缩）
   - 判断误判（起点差距看变化不看重值/接近≠超过）
   - 审题偏差（问增速答增量、问环比看同比、月均忘除月份数）
   - 7秒盲选（用时极短，未展开思考）

3. 入库错题
   - 调 mistake_manager.create_mistake(question_type, knowledge_point, error_reason, correct_answer, source)
   - 更新 index.md（index_manager.add_entry）
   - 入库后回复格式：

     ✅ **错题-XXX 已入库**
     | 考点 | {考点} |
     |------|--------|
     | 错因 | {错因一句话} |
     | 正确 | {正确选项} |
     | 你选 | {错误选项} |

     > {核心口诀/推论}

4. 检查公式速查表是否覆盖
   - 加载本技能的 references/资料分析-公式速查.md
   - 若该考点/推论不在速查表中 → 立即追加写入
   - 同时检查技能副本 vs 用户实际文件（/mnt/e/obsidianNote/考公/参考书籍/资料分析常用公式速查.md），确保同步

5. 给出针对性口诀
   - 必须包含可行动的"停一步"规则
   - 必须与用户历史错题中相同考点产生连接（"这次和错题-XXX是同一类"）
```

### 输出规范

每条回答控制在 5-8 行，包含：
1. ✅ 入库确认（一行）
2. 考点+错因+正误对照表（2-3 行）
3. 核心口诀/推论（1-2 行）
4. （可选）与历史错题关联（1 行）

**用户偏好记录：**
- 该用户要求每个错题分析必须包含一个可背诵的"口诀"或"停一步"触发规则
- 该用户对概念类混淆的错题尤其敏感，回复应突出"本质区分"（如 总量÷总量 vs 增量÷增量）
- 该用户希望分析直接、聚焦，不展开过多背景信息
- **反馈格式偏好**：该用户接受「双重诊断」格式——先肯定做对的（✅ 做得对的），再指出要解决的（⚠️ 还要解决的），最后给3个具体动作
- **截图展示偏好**：用户说"从错题库抽取XX截图"/"错题的截图呢"时，要求直接用路径展示原图（MEDIA:），不能用文字重建或文字描述替代。没有截图的题才走文字重建。vision_analyze 只用来读取图片内容做分析，分析结果不能替代原图展示。

**复习模式核心价值**：该用户记得自己错题的答案（亲手录入+截图+入库三步都参与了），复习的真正收益来自**每道题的讲评（正解/错因/口诀/历史关联）**，而非单纯回忆测试。出题后应逐题讲评，每次提取一句口诀或「停一步」规则。\n- **图形推理复习**：考「考点识别」而非计算——描述特征问应优先用什么规律，选项为属性/数量/位置/样式四类。\n- **逻辑判断复习**：考「力度比较」而非推理——搭桥>排除他因>举例>重复论点；实验类他因削弱>断桥>可能性削弱。\n- **资料分析复习**：考「概念区分」和「抄错原因排查」——着重分辨占比vs贡献率、比重差vs平均增速、多几倍vs是几倍、累计vs当月。
- **资料分析复习**：优先引用用户创建的两个文件：
  1. `/mnt/e/obsidianNote/考公/参考书籍/资料分析公式与抄错原因速查.md`（公式+抄错原因二合一）
  2. `/mnt/e/obsidianNote/考公/参考书籍/资料分析表格类错题整理.md`（十一类表格操作错误专题）
  两者配合使用，公式速查覆盖计算层面，表格整理覆盖操作层面。无需维护 skill 内 references 副本。

  2026-07-15 新增 vault 文件：
  3. `/mnt/e/obsidianNote/考公/参考书籍/资料分析公式汇总.md`（11 章节完整公式表，适合快速查阅）
  4. `/mnt/e/obsidianNote/考公/参考书籍/资料分析·你的专属易错公式卡.md`（7 大高频陷阱 + 错题-278~287 真实案例，专治多几倍-1/累计当月/贡献率等高频翻车点）
  场景选择：全面复习用文件 1，快速查阅用文件 3，专项攻克易错用文件 4，表格题操作用文件 2。

---

### MCP 调用总量控制（Workflow 5 全局）

| 模式 | 最大 MCP 请求次数 | 说明 |
|------|:---:|------|
| 完整指南模式 | 20 次 | 每考点 2-3 次，最多 8 个考点 |
| 速查模式（每条消息） | 1 次 | search 一次，必要时追加 1 次 files/content |
| 题型方法论模式 | 5 次 | 2 次 search + 最多 3 次 files/content |

**请求失败处理**：同 Workflow 4，静默跳过，输出末尾加 `⚠️ 知识库检索暂时不可用`，本地错题数据继续正常展示。

---

---

## Workflow 6：批量从对话入库（讨论→入库模式）

---

## Workflow 7：错题截图抽取模式（默认零文字输出）

### 触发条件

- 用户说"出N题"/"再出N题"/"抽N题错题"/"抽XX题型错题"/"给我出N题"
- 用户说"错题的截图呢"/"图呢"（用户想看原图，不是文字描述）
- 用户说"复习" + 题型名称（如"复习判断推理"）

### ⚠️ 用户核心偏好（2026-07-23 明确纠正，不可违反）

**当用户要求从错题库出题时，输出必须遵守：**

1. **只发原图，零文字** — 不发考点说明、不发题目重建文字、不发错因分析、不发口诀、不发任何解释性文字
2. **截图文件存在时直接展示** — 用 MEDIA: 或文件路径直接引用，不经过 browser_navigate + browser_vision 渲染，不经过 vision_analyze 读取后再描述
3. **无截图不上** — 若截图文件不存在，直接跳过该题，不文字重建、不替代展示
4. **不发汇总表** — 不发 ID/考点/答案对照表、不发陷阱分析、不判题、不更新复习状态
5. **等用户答完再响应** — 用户把答案发回来后，才做判题+讲评+状态更新

**一句话记忆：** 「题图直出→用户自己做→用户发答案→我再判」。不发考点/错因/口诀/汇总表。

### 与 Workflow 2（复习模式）的区别

| 维度 | Workflow 2（复习模式） | **Workflow 7（截图抽取）** |
|------|----------------------|--------------------------|
| 触发 | 说"复习"/"判题" | 说"出N题"/"再出N题"/"抽题" |
| 输出方式 | 文字+截图混合 | **纯截图，零文字** |
| 判题时机 | 用户答完后立即判 | **用户答完后才判** |
| 无截图处理 | 文字重建考点识别题 | **跳过（无截图不展示）** |

### 执行步骤

**Step 1：确定抽取范围**

```
1. 用户指定题型（如"判断推理"）→ 只该题型
2. 用户指定数量（如"10题"）→ 抽 N 题
3. 未指定题型 → 默认该用户最高频题型（判断推理）
4. 未指定数量 → 默认 10 题
```

**Step 2：筛选可用条目**

从 index.md 读取 entries，过滤：
- question_type == target_type（如有指定题型 或 默认判断推理）
- status != "mastered"
- **必须有截图文件**（screenshots 目录下有 {id}.png / {id}.jpg）

随机抽取 N 题（不按到期排序）。

**截图存在性检查（重要！）：**
```
检查顺序：
  1. {题型}/screenshots/{id}.png
  2. {题型}/screenshots/{id}.jpg
  3. {题型}/screenshots/{id}.jpeg
任一存在即视为有截图。
```

无可用条目时输出："{题型}中没有带截图的待复习错题"并停止。

**Step 3：截图展示（纯截图，零文字）**

直接通过文件路径展示截图——不裁剪、不渲染、不描述：

```
MEDIA:/mnt/e/obsidianNote/考公/错题库/{题型}/screenshots/错题-{ID}.{ext}
```

一次展示 N 张截图在一起，不加任何编号、标题、说明文字。

**⚠️ 严禁行为：**
- ❌ 不写"题1"、"题2"等编号
- ❌ 不写"考点：XXX"、"错因：XXX"
- ❌ 不写"请作答"、"请回复答案"
- ❌ 不写汇总表、陷阱分析
- ❌ 不使用 browser_navigate + browser_vision 渲染（大图易超时，且用户要的是原图不是渲染截图）
- ❌ 不使用 vision_analyze 读取后再用文字描述

### Step 4：等待用户作答

展示完截图后不说话，等用户把答案发回来。

### Step 4b（子流程）：用户说「不记得」+「存入艾斯浩宾」→ 批量 fail 状态更新

触发条件：展示截图后，用户的回复是以下模式之一：
- 「错题-XXX 不记得」「XX题不记得」「忘了」「没印象」
- 「存入艾斯浩宾」「存入」「保存状态」「记录进去」

注意：这个子流程和下方 Step 5 的常规判题流程是**二选一的互斥路径**——
如果用户说了「不记得」+「存入」，走此子流程；如果用户给了答案序列（如「AACBD」），走 Step 5。

执行步骤：

1. **解析用户提到的错题 ID**：
   - 从用户消息中提取数字/ID（如「错题-116 错题-160」→ [116, 160]）
   - 若用户只说「存入」没说具体 ID，则**从上一次发送的截图中取全部 ID**

2. **批量更新（推荐用 Python 脚本一次处理全部，避免 20+ 次单独工具调用）**：

   写一个临时 Python 脚本 `/tmp/update_reviews.py`，一次性处理所有 `.md` 文件：
   ```python
   import json, re, os
   from datetime import datetime, timedelta
   
   today = "YYYY-MM-DD"
   results = {"eid": "fail", ...}  # 全部 fail
   base = "/mnt/e/obsidianNote/考公/错题库/{题型}"
   
   for eid, result in results.items():
       path = os.path.join(base, f"错题-{eid}.md")
       with open(path, 'r', encoding='utf-8') as f: text = f.read()
       fm = re.search(r'^---\n(.*?)\n---', text, re.DOTALL).group(1)
       cur = int(re.search(r'^review_stage:\s*(\d+)', fm, re.M).group(1))
       
       if result == "fail":
           new_s, new_n = 0, (datetime.strptime(today, "%Y-%m-%d") + timedelta(1)).strftime("%Y-%m-%d")
       else:
           new_s = min(cur+1, 6)
           intervals = [0, 1, 2, 4, 7, 15]
           new_n = (datetime.strptime(today, "%Y-%m-%d") + timedelta(intervals[new_s])).strftime("%Y-%m-%d")
       
       fm = re.sub(r'^review_stage:\s*\d+', f'review_stage: {new_s}', fm, flags=re.M)
       fm = re.sub(r'^next_review:\s*\S+', f'next_review: {new_n}', fm, flags=re.M)
       hist = json.loads(re.search(r'^review_history:\s*(\[.*?\])', fm, re.DOTALL).group(1))
       hist.append({"date": today, "result": result, "stage": cur, "next_review": new_n})
       fm = re.sub(r'^review_history:\s*\[.*?\]', f'review_history: {json.dumps(hist, ensure_ascii=False)}', fm, flags=re.DOTALL)
       
       body = text[re.search(r'^---\n.*?\n---', text, re.DOTALL).end():]
       with open(path, 'w', encoding='utf-8') as f: f.write(f"---\n{fm}\n---\n{body.lstrip()}\n")
   ```
   `.md` 文件更新完后再用 sed 批量更新 index.md 对应行。

3. **输出结果汇总**：

```
✅ 已记入艾斯浩宾

| 错题 | 原阶段 | 现阶段 | 下次复习 |
|:----:|:-----:|:-----:|:--------:|
| 错题-116 | 1/6 | 0/6 ❌ | 明天 |
| 错题-160 | 0/6 | 0/6 ❌ | 明天 |
```

**⚠️ 注意：** 这个子流程只更新状态，不判题、不讲评、不给口诀（用户已经明确说了「不需要发文字」）。只输出三行汇总表 + 后续的复习日期即可。

---

### Step 4c（子流程）：用户批量作答「XX题不记得 其他通过」→ 混合 fail/pass 状态更新

触发条件：展示多张截图后，用户的回复模式如下：
- 「错题-XXX 不太记得 其他通过」
- 「XX题不记得 其他记得」
- 「XXX忘了 其他都记得」
- 「XXX没过 其他过了」

核心模式：用户**不逐题给答案字母**，而是用一种**二元分类**的方式评价整批题——哪些题不记得（fail），哪些通过（pass）。

**与 Step 4b 的区别**：Step 4b 是**全部 fail**（全部不记得/存入），Step 4c 是**混合 fail + pass**（部分不记得，部分通过）。两者在处理完用户提到的 fail 题后，还需处理剩余的 pass 题。

执行步骤：

1. **解析 fail 题**：
   - 从用户消息中提取包含「不记得/忘了/没过」等关键词的错题 ID
   - 常见格式：`「错题-055 不太记得」「错题-055不记得」「055没过」`

2. **确定全部题池**：
   - fail 题 = 上一步解析出的 ID
   - pass 题 = **上一轮发送的全部截图 ID 减去 fail 题**
   - 例如：发了 10 张截图，用户说「055不太记得 其他通过」→ fail=[055]，pass=[062,071,085,...]

3. **批量更新（推荐用 Python 脚本一次处理全部，避免 20+ 次单独 patch 调用）**：
   - 写一个临时 Python 脚本 `/tmp/update_reviews.py`
   - 对 fail 题：`fail_review()` → stage=0, next_review=明天
   - 对 pass 题：`pass_review(old_stage)` → stage+1, next_review=today+interval[new_stage]
   - 同时更新 `.md` frontmatter + review_history + 底部复习记录表 + index.md 对应行
   - 注意：`write_file` 会完全覆盖文件，确保写入前已读取完整内容

4. **输出结果**：

```
## 📊 本轮N题复习结果

| 错题 | 考点 | 结果 | 阶段 | 下次复习 |
|:---:|:----|:---:|:---:|:--------:|
| 错题-055 | 判断推理 | ❌ 不记得 | 0→0 | 07-24（明天） |
| 错题-062 | 判断推理 | ✅ 通过 | 1→2 | 07-25 |
| ... | ... | ... | ... | ... |

> **通过率：N/M（X%）** | 错题-XXX 明天到期复习
```

**⚠️ 注意：** 和 Step 4b 一样，只更新状态 + 输出汇总，不判题、不讲评、不给口诀。这是 Workflow 7（纯截图抽取）与 Workflow 2（复习判题）的核心区别。

---

**Step 5：用户提交答案后 → 判题+讲评+状态更新**

用户给出答案序列（如 "AACBD DCABA"），然后执行：
1. 逐题对答案
2. 每题的 ✅/❌ + 考点 + 错因一句话 + 口诀
3. 更新 index.md 复习状态（艾宾浩斯）
4. 输出通过率统计

---

### 触发条件

- 用户说"帮我把以上题目都入库"/"把这些题入库"/"都入库"/"入库题目"
- 用户与你在对话中讨论了多道题（截图/文字），逐一给出正解和错因，然后要求一次性全部入库

### ⚡ 核心行为规则：立即执行，不犹豫

**当用户说「入库题目」时，如果截图和错因分析已在对话上下文中（刚刚分析完），必须立即批量导入，不要：**
- ❌ 先去检查目录结构（ls/树状图）
- ❌ 先去读现有 index.md 确认 last_id
- ❌ 先问用户"确认要入库吗"
- ❌ 先解释步骤流程

**正确动作：** 直接在回复中告知将分配哪些 ID，然后写脚本一次性全部写入。如果缺少某道题的完整信息（如选项、答案未显示），跳过该题并在汇总中说明，不等用户补充。

> **记忆口诀：** 截图在手+分析已做=马上写脚本，不查目录不问人

### 与 Workflow 1（单题）和 Workflow 3（截图目录批量）的区别

| 维度 | Workflow 1（单题） | Workflow 3（截图目录） | **Workflow 6（对话批量）** |
|------|------------------|---------------------|--------------------------|
| 数据来源 | 用户单条消息 | 截图目录 | **对话上下文（已讨论过的题）** |
| 错因提取 | 用户给出 | OCR + 关键词映射 | **从对话中已分析的结果提取** |
| 考点 | 用户给出 | 关键词映射（易错） | **从对话中的分析结论提取（准确）** |
| 答案 | 用户给出 | OCR（手写答案~30%） | **对话中已确认** |
| 截图 | 用户提供 | 目录扫描 | **对话中的截图已存在** |

### 执行步骤

**Step 1：确认题量，分配 ID**

快速扫对话找题目数量，回复确认格式：
```
好的，共 N 题要入库。从 错题-{起始编号} 开始分配。
```

**Step 2：从对话提取每道题的数据**

每条从对话中提取：

| 字段 | 提取来源 |
|------|---------|
| 题型 | 对话中的题型讨论 |
| 考点 | 对话中分析的考点（如：削弱-拆桥/集合推理/且的逆否） |
| 错因 | 对话中分析的错因——**一句话写清楚**，不用"待补充" |
| 正确答案 | 对话中确认的正确选项 |
| 用户答案 | 用户实际选的选项（如有） |
| 来源 | 题目来源——对话中可能有，没有则用"专项智能练习" |

**核心原则：** 对话中已经分析完了，所以错因、考点、口诀全部是确定的。不允许写入"待补充"。

**Step 3：构造每题的 .md 内容**

格式模板：

```
---
id: 错题-XXX
date: {当天}
题型: {题型}
考点: {考点}
错误原因: {一句话错因}
正确答案: {正确选项}
用户答案: {用户选项}（可选）
来源: {来源}
review_stage: 0
next_review: {当天+1天}
review_history: []
status: pending
---

## 标签

- **题型**：{题型}
- **考点**：{考点}
- **错误原因**：{一句话错因}
- **正确答案**：{正确选项}
- **用户答案**：{用户选项}
- **来源**：{来源}

## 出题人陷阱

**{陷阱类型名称}**

{手法拆解——题干说了什么 vs 选项/结论实际说了什么}

## 解题口诀

> **{一行可背诵的口诀}**

## 复习记录

| 日期 | 结果 | 阶段 | 下次复习 |
|------|------|------|----------|
| {当天} | 入库 | 0/6 | {次日} |
```

每道题都要包含：
- ✅ 陷阱类型名称（如"概念偷换"、"且→或逆否漏项"、"近义词偷换"）
- ✅ 解题口诀（一行背诵）
- ✅ 题干vs选项的对比表格或对比说明

**Step 4：批量创建文件 + 更新索引**

```
for each question:
  → write_file({题型}/错题-{ID}.md, content)
  → 如有截图，复制到 {题型}/screenshots/
end

→ patch index.md：
  · last_id += N
  · total += N
  · pending += N
  · 在对应题型的最后一行后插入 N 行
```

**Step 5：跨题陷阱模式提取（关键！）**

批量入库完成后，**立即检查这批题是否有共同陷阱模式**：

```
从刚入库的每道题的"出题人陷阱"节提取陷阱类型名称
如果有 ≥2 题陷阱类型相同或相似：
  → 合并为一条通用陷阱规则
  → 追加写入 references/判断推理-出题人陷阱模式.md
如果有孤立陷阱（只有 1 题出现）：
  → 也作为独立条目写入，未来可能和其他题合并
```

**为什么这是必要的：** 该用户的学习方式是通过**模式识别**——每道题记住具体的陷阱名称，比记住"这题选B"有用得多。把每批题的陷阱提取到参考文件中，下次复习时可以批量调用陷阱清单做快速回顾。

**写入格式：** 与原参考文件中已有的陷阱条目一致，每条包含：
- 陷阱名称（如"概念偷换"、"且→或逆否漏项"）
- 手法说明
- 防御口诀（一行可背诵）
- 对应错题ID索引

**先更新参考文件，再输出汇总表。**

---

**Step 6：输出汇总表**

入库完成后，给用户一个汇总：

```
全部入库完成 ✅

| ID | 考点 | 错因一句话 | 正解 | 你选 |
|:--:|:----|:----------|:---:|:----:|
| 错题-289 | 削弱-拆桥 | 结论说"乳酸菌"大类，实验做"植物乳酸菌"——概念偷换 | C | B |
| 错题-290 | ... | ... | ... | ... |

**索引更新：**
- last_id: {原值} → {新值}
- total: {原值} → {新值}
- pending: {原值} → {新值}
```

### 注意事项

- **先建文件再改索引**——避免索引指向不存在的文件
- **对话中的答案如果先选错后知道正解**——`correct_answer` 写入正解，用户初选用用户答案字段记录
- **错因必须一句话写清楚**——不要用"待补充"，对话中已经分析了
- **每道题必须有一个解题口诀**——这是该用户的硬性要求
- **索引插入时注意 table 格式一致性**——检查现有行的管道格式，保持统一

---

## 辅助操作

### 查看进度（stats）

```python
# cli._cmd_stats()
data = index_manager.load_index()
grouped = data.entries_by_type()
# 输出 total / pending / mastered / updated + 各题型分布
```

### 修改错题字段（modify）

可修改字段：`knowledge_point` / `error_reason` / `correct_answer` / `source` / `question_type`

若修改 `question_type`：自动将 `.md` 和截图移动到新题型目录，重写 index.md。

### 删除错题（delete）

删除 `{题型}/{ID}.md` + `{题型}/screenshots/{ID}.png`，从 index.md 移除对应行。

---

### 重建索引（index sync）

**触发条件**：`index.md` 中某题型条目数与目录下 `.md` 文件数不一致，或怀疑索引数据陈旧。

**执行步骤**：

```python
import index_manager, mistake_manager, os

qt = "资料分析"  # 目标题型
base = config.MISTAKE_ROOT / qt
mds = sorted([f for f in os.listdir(base) if f.endswith(".md")])

for md in mds:
    eid = md.replace(".md", "")
    if index_manager.find_entry(eid):   # 已存在，跳过
        continue
    card = mistake_manager.load_card(eid, qt)
    if card:
        index_manager.add_entry(card)
```

**原理**：扫描目标题型目录的所有 `.md` 文件，对 index 中不存在的条目调用 `load_card()` 读取 frontmatter + 标签，再 `add_entry()` 写入 index.md。`find_entry()` 按 ID 精确匹配，不会重复添加。

> ⚠️ `load_card()` 从 `.md` frontmatter 读取的数据可能与上次操作后的实际状态一致（因双写机制失效导致 drift），但这是唯一可用的恢复数据源。

---

## 数据一致性规则（V2 — 分索引）

1. **分索引是调度数据源**——复习模式优先读对应题型的分索引（如 index-判断推理.md），不扫全部 `.md` 文件
2. **写操作必须写分索引 + .md 文件**——每次增删改都同步更新对应题型的分索引和 `.md` 文件
3. **主 index.md 自动生成**——每次写分索引后自动聚合生成主 index.md 总览页，不要手动编辑
4. **modify 用 delete+add 重建分索引行**——保证字段一致性
5. **save_category_index 自动重算统计**——`total / pending / mastered / updated` 在 `save_category_index()` 内自动重算

---

## 配置速查（config.py）

| 变量 | 默认值 | 环境变量覆盖 |
|------|--------|-------------|
| `OBSIDIAN_ROOT` | `E:\obsidianNote\考公` | `EXAM_OBSIDIAN_ROOT` |
| `EXAM_DATE` | `2026-11-01` | `EXAM_EXAM_DATE` |
| `OCR_API_URL` | `https://api.siliconflow.cn/v1/chat/completions` | — |
| `OCR_MODEL` | `deepseek-ai/DeepSeek-OCR` | — |
| `OCR_API_KEY` | config 内默认值 | `EXAM_OCR_API_KEY` |
| `REVIEW_BATCH_SIZE` | `5` | — |
| `MASTERED_STAGE` | `6` | — |
| `EBBINGHAUS_INTERVALS` | `[0,1,2,4,7,15]` | — |
| `FEISHU_WEBHOOK_URL` | `""` | `EXAM_FEISHU_WEBHOOK` |

---

## ⚡ 批量效率提示\n\n- **批量更新艾宾浩斯用 Python 脚本，不要逐条 patch**：当用户一次性评价 10 道题（如「055不记得 其他通过」），写一个临时 Python 脚本 `/tmp/update_reviews.py` 一次性处理全部 10 个 `.md` 文件。单独 patch 调用 10 次 + index 更新 10 行 = 20+ 工具调用；一次 Python 脚本 = 1 次 `write_file` + 1 次 `terminal`。\n\n## 已知限制与注意事项

- **红笔手写答案 OCR 不可靠**：印刷体准确率高，手写答案识别率约 30%，批量导入后建议人工逐题校验 `correct_answer` 字段
- **判题不核对字母**：`_is_forget()` 只判断「是否记得」，不做 A/B/C/D 对错比较——只要能描述思路就算通过
- **单字母回复视为失败**：`len(answer) <= 1` 判定为没有实质内容，需写至少两字以上的描述才算通过
- **dry_run 不入库**：`import-batch --dry-run` 执行 OCR 但不写文件，仅返回预览报告
- **import_directory 的 success 统计**：当前实现 `success = total`（只要没抛异常就算处理），OCR 失败的错误信息会写入 `.md` 的 OCR 文本节，不会中断批量流程
- **修改 question_type 后 last_id 不变**：题目移动到新题型目录后 ID 保持原值不重新编号
- **index.md 解析依赖正则**：`_TABLE_ROW_RE` 要求 8 列管道表格，手动编辑 index.md 时需保持格式一致，否则行解析会静默跳过
- **Wiki MCP 知识巩固不阻塞入库**：MCP 调用在 Workflow 1 写文件全部完成后才发起，任何网络异常只影响知识巩固输出，错题数据已安全落盘
- **Wiki MCP search 的 topK 上限**：两次搜索各取 topK=3，合并去重后最多展示 4 条，不建议调高以免 context 溢出
- **历史同类错题匹配为精确匹配**：`error_reason` 字段完全相同才命中，若用户填写「知识点盲区」和「知识盲区」会被视为两类，建议统一使用预置错误原因词表
- **Workflow 5 完整指南模式的 MCP 请求上限为 20 次**：超出考点数量时合并到相近考点，避免单次对话 MCP 调用过多导致超时
- **Workflow 5 速查模式每条消息只发 1 次 MCP 搜索**：用户在冲刺状态，响应速度优先于内容完整性，files/content 追加读取仅在片段 < 150 字时触发
- **距考试天数计算**：从 `config.EXAM_DATE`（默认 2026-11-01）与今日日期之差自动计算，显示在完整指南标题行
- **WSL 环境路径陷阱**：config.py 默认 `OBSIDIAN_ROOT = r"E:\obsidianNote\考公"`（Windows 路径）。在 WSL 下 Python 的 `Path("E:\...")` 不会自动映射到 `/mnt/e/...`，导致 CLI 写入错误的空目录。**必须在每次 CLI 调用前设置环境变量**：`EXAM_OBSIDIAN_ROOT=/mnt/e/obsidianNote/考公`。忘记设置的表现：CLI 输出「错题-001」但 index.md 的 last_id 远大于 001

- **⚠️ Python 正则批量更新 index.md 的格式陷阱（2026-07-26 真实坑）**：

  **坑一：前导双管道符（||）** — index.md 某些行以 `|| 错题-XXX` 开头而非 `| 错题-XXX`，这是之前不同的入库脚本写入格式不一致导致的。正则匹配 `| 错题-XXX` 会漏掉这类行。

  **坑二：行内嵌入 \\n 字面量** — 某些行内嵌了 `\\n` 字符串（非实际换行），导致一行包含两条记录，grep 和 split 都会出错。

  **解决方案：**
  1. 查行时先用 `grep -n "错题-XXX"` 确认精确格式，再手动指定替换文本
  2. 替换时匹配该行的完整文本，不做模糊/部分匹配
  3. 若用 Python 脚本，避免 `re.sub` 全局替换，改用逐行 `split("|")` → 逐列更新 → `join("|")` 重建
  4. 检查：更新后对所有修改行 grep 管道符数量 = 7（8 列）。≠7 → 格式损坏，回退重做

  **根本原因：** 早期入库脚本在写入 index.md 时用了不一致的格式（有的加前导 `||`，有的不加），后续手动 patch 修复后又引入 `\\n` 嵌入问题。建议统一用 Python 脚本的 `index_manager.add_entry()` 写入新行，不要手动 patch。

---

## 📎 参考文件

| 文件 | 内容 |
|------|------|
| `references/图形推理-九宫格.md` | 九宫格题型方法论：排查顺序（遍历→位置→数量→竖列→对角线）、三种典型题型速判、考场速记口诀 |
| `references/图形推理-通用入口口诀.md` | 图形推理通用入口决策树（相同→位置|相似→样式|不同→先属性后数量|特殊→空间）；四大专项突破；高频错因与考场口诀 |
| `references/资料分析-公式速查.md` | 资料分析14类常用公式+高频推论（同比判环比、比重趋势判增速、两期比重逆用、三大率对比、读图检查清单） |
| `references/资料分析-表格类错题整理.md` | 指针指向用户 vault 中的 `/mnt/e/obsidianNote/考公/参考书籍/资料分析表格类错题整理.md`（十一类表格操作错误专题，含两期比重表、数据查找、缺项推算、饼图混读、双表联用等） |
| `references/工作流6-直播模考逐题分析.md` | 直播模考截图逐题分析5步法：截取信息→定位考点→入库→同步速查表→给口诀 |
| `references/资料分析-三大陷阱防御.md` | 2026-07-15 新增。句式陷阱/计算错误/图表对应错误三大防御框架 + 7项自检清单 + 六句诀。源自用户实战诊断：难度4.0只拿到40%准确率。 |
| `references/判断推理-高频考点规律速查.md` | 2026-07-15 新增。面数量(生活化图形)、功能元素(线上vs交点/方位左右)、黑白运算(三色/双色)、空白区域对称轴旋转、搭桥vs排除他因力度比较、实验类他因削弱，每考点附口诀+用户错题案例。 |
| `references/资料分析-纯增长率公式速查.md` | 2026-07-23 新增。5 个只需增速不需绝对量的公式（平均数增长率/比重增速/间隔增长率/乘积增长率/定性判断），含考场四步判表+速记口诀。 |